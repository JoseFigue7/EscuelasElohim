# Desarrollo local — copiar a backend/.env
# cp .env.example backend/.env

# Django
SECRET_KEY=superclavesecretade50caracteres-estaclaveesupersecretayaleatoria
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (Docker Compose o PostgreSQL local)
DB_NAME=elohimcoban_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# CORS / CSRF (frontend en puerto 3000)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
